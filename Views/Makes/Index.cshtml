@model IEnumerable<DesertStorm.Models.Make>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MakeTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MakeTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MakeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MakeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MakeID })
        </td>
    </tr>
}

</table>
